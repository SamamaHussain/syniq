import 'package:flutter/material.dart';

class JobDescPage extends StatelessWidget {
  const JobDescPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
